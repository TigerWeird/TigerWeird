const Discord = require("discord.js");
const { ee, ev, npn, nps } = require("../../Src/Json/used.json");

module.exports = {
  name: "suggest",
  alias: ["sug", "sugerir", "sugerencia"],
  botPerms: [],
  userPerms: [],

async execute (client, message, args, guildC, reply, editReply, send) {
  }
}